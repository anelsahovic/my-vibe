export type EventSmallCardType = {
  id: string;
  name: string;
  location: string;
  startDate: Date;
  price: number;
};

export type UserSmallCardType = {
  id: string;
  name: string;
  username: string;
  image?: string;
};
